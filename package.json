{
  "name": "Master Test",
  "description": "Установка окружения: \r npm install -g protractor@5.2.0\r webdriver-manager update",
  "main": "conf.js",
  "dependencies": {
    "protractor": "5.2.0",
    "protractor-jasmine2-screenshot-reporter": "^0.5.0",
    "jasmine2-custom-message": "^0.8.2"
  },
  "scripts": {
    "install-env": "npm install protractor@5.2.0 -g && webdriver-manager update && npm install",
    "wm-start": "webdriver-manager start",
    "test": "protractor conf.js"
  },
}